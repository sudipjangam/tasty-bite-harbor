
import React from 'react';

// This is now just a simple wrapper to avoid breaking any existing imports
// All chart functionality has been migrated to Highcharts
export const XAxis: React.FC<any> = (props) => {
  return null;
};

export const YAxis: React.FC<any> = (props) => {
  return null;
};
